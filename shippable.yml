language: node_js
node_js:
  - "node"
install:
  - npm update -g npm
  - java -version

build:
  ci:
    - npm install
    - npm run lint

notifications:
  email:
    on_success: change
    on_failure: always
